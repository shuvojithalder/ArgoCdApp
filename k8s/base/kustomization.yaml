apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd-app

commonLabels:
  app.kubernetes.io/managed-by: argocd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: argocd-app
    newName: ghcr.io/OWNER/ArgoCdApp
    newTag: latest
