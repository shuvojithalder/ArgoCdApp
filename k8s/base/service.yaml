apiVersion: v1
kind: Service
metadata:
  name: argocd-app
  labels:
    app.kubernetes.io/name: argocd-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: argocd-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
