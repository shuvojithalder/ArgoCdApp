apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: service-nodeport.yaml
  - path: deployment-replicas.yaml
  # Uncomment if GHCR package is private (see docs/fix-imagepullbackoff.md):
  # - path: patches/ghcr-pull-secret.yaml
