apiVersion: v1
kind: Service
metadata:
  name: argocd-app
  namespace: argocd-app
spec:
  type: NodePort
  selector:
    app: argocd-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
